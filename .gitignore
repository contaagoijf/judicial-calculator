# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# Saida gerada pelo Playwright (npx playwright test)
/playwright-report/
/test-results/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ferramenta de automação de sessão (nao deve ser commitada)
.glpi-driver.mjs
glpi-profile/

# Scripts avulsos de uso local (ex.: migrações manuais de banco), nunca versionados
src/script/

# Material de apoio para reuniões com o contador (nunca versionado, qualquer extensão)
docs/contadoria/

# Arquivos .txt, .docx e .pdf não são versionados (documentos gerados
# localmente), exceto o robots.txt, que é servido de fato pela aplicação em
# produção.
*.txt
*.docx
*.pdf
!public/robots.txt

# Arquivos e pastas de tooling de agente de IA (nunca versionados)
AGENTS.md
.agents/
docs/adr/template.md
.claude/
CLAUDE.md
